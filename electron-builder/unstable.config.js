const config = require('./stable.config.js');

config.appId = 'jp.nicovideo.nair-unstable';
config.productName = 'N Air Unstable';
config.extraMetadata.name = 'n-air-app-unstable';

module.exports = config;
