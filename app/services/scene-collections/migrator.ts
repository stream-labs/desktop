import electron from 'electron';

/**
 * Migrates from V1 scene collections (SceneConfigs directory)
 * to V2 (SceneCollections with manifest file).
 * Maybe some day we can delete this.
 */
export class SceneCollectionsMigrator {

  migrate() {
    // TODO: Migrate to the new system
  }

}

